<a href="#stores" class="{{#if hasClass}} hidden {{className}} {{else}}storelocator-accesspoints-headerlink-link hidden{{/if}}" data-touchpoint="{{touchpoint}}" data-hashtag="#stores" title="{{translate 'Store Locator'}}">
	<i class="storelocator-accesspoints-headerlink-link-icon"> </i>
	<span class="storelocator-accesspoints-headerlink-link-text"> {{translate title}}</span>
</a>



{{!----
Use the following context variables when customizing this template: 
	
	title (String)
	hasClass (Boolean)
	touchpoint (String)

----}}

